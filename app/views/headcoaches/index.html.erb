<% @page_title = "ヘッドコーチ一覧" %>
<h1><%= @page_title %></h1>

<button type="button"><%= link_to "HC登録", :new_headcoach %></button>

<table>
  <tr>
    <th>名前</th>
    <th>年齢</th>
    <th>出身</th>
  </tr>
  <% @headcoaches.each do |headcoach| %>
  <tr>
    <td><%= headcoach.full_name %></td>
    <td><%= headcoach.age %></td>
    <th><%= headcoach.college %></th>
  </tr>
  <% end %>
</table>
